# EXPERIMENTS.md — Experiment Strategy & Status

Living document tracking the overarching experimental plan and where each piece stands.
Companion to [CLAUDE.md](CLAUDE.md) (design/spec) and [hpc/experiment_setup.md](hpc/experiment_setup.md)
(compute budget). When in doubt about *what we are running and why*, this file is authoritative.

---

## 0. Overall Goal

Show, **through experiments, which RL approach is most suitable** for this infrastructure-maintenance
MDP. Strategy: **start small, then scale up**, across three experiments:

| Experiment | Instance | Scope | Replication |
|---|---|---|---|
| **Exp 0 — Exploration** | `instance_10p` (N=10) | Wide screen of *all* algorithm variants; cheap, just to see what works | **1 per config** |
| **Exp 1 — Wide selection, larger instance** | ~N=30 (likely Sioux Falls, ~30 projects) | The subset of approaches that worked in Exp 0, run for real | 5 seeds (stats) |
| **Exp 2 — Scale-up case study** | Large case study (TBD) | Only the best algorithms from Exp 1 | 5 seeds (stats) |

Exp 0 is exploratory: a single replication per config is enough to spot which variants are viable and
prune the grid. Statistical claims (rliable bootstrap CIs) come from Exp 1 onward, where replication
matters. Each step is small enough to be tractable before committing compute to the next.

---

## Experiment 0 — Exploration on `instance_10p`

> **⚠️ Instance regenerated 2026-06-19 — exp0 re-run in progress.** A predictability analysis
> ([docs/i10p_predictability_analysis.md](docs/i10p_predictability_analysis.md)) found the original
> `instance_10p` was **noise-dominated**: failure timing was essentially unpredictable (time-to-failure
> CV ≈ 0.6; current state explained only R²≈0.58 of cost-to-go), which *structurally caps* anticipatory
> RL — explaining why MC rollout barely beat reactive on the mean. `instance_10p.json` was regenerated
> to be **anticipatable** (`alpha0_mean` 0.05→0.8 → TTF-CV ≈ 0.22, same 60-yr lifetime); the old one is
> saved as `instances/instance_10p_noisy_alpha0p05.json`. **The first exp0 results (on the noisy
> instance) are archived at `results/exp0_noisy_alpha0p05/` — they are the "unpredictable" arm and a
> finding in their own right** (notably: MC rollout cut outcome-cost **CV 0.74→0.23** — its value was
> *risk reduction*, not mean anticipation → report CVaR/P90, not just mean). Exp 0 is now being re-run
> on the predictable instance.

All Exp 0 runs use `instances/instance_10p.json`, Sioux Falls network, `fast` TAP backend.
Evaluation uses shared CRN across agents (paired comparison), so costs are directly comparable.
**One replication per config** — exploratory only, no aggregate statistics.

> **0A → 0B is a pipeline, not two parallel batches.** The Optuna heuristics (0A) must run **first**:
> their best-found parameters are the heuristic that the 0B agents consume (ADP warmstart policy,
> MC-rollout base policy). Submit 0A, read each run's `best_params.json`, inject those values into the
> relevant 0B configs, *then* submit 0B. See [Gaps](#gaps--open-items) for the current wiring status.

### 0a. Optuna-tuned heuristics (baselines **and** input to 0B)

Tuned classical policies that (1) set the baseline the RL methods must beat and (2) **supply the
heuristic that the 0B agents are warm-started / rolled out from**. **Single-threaded** (see Compute /
conventions). Each run writes `results/<run>/best_params.json`.

> ⚠️ The Optuna `best_value` is **optimistically biased** (~1.8x) vs. held-out cost — it is the min
> over many noisy trials (winner's curse), so **always report the held-out `"evaluation"` metric**, not
> `best_value`. To mitigate, tuning now uses `WilcoxonPruner` + CRN across trials and **100 tuning
> episodes** (was 30); see [hpc/registry_conventions.md](hpc/registry_conventions.md).

| Config | Heuristic |
|---|---|
| [i10p_optuna_reactive.json](configs/i10p_optuna_reactive.json) | reactive (threshold) |
| [i10p_optuna_paced.json](configs/i10p_optuna_paced.json) | paced |
| [i10p_optuna_perasset.json](configs/i10p_optuna_perasset.json) | reactive-per-asset |

### 0b. Heuristic-fed / learning algorithms

These either warm-start from, bootstrap off, or roll out a heuristic policy — **the heuristic and its
parameters should come from 0A's tuned output** (currently placeholders, see Gaps).

**(i) ADP + variants — full grid.** Dimensions:
- **Action generation:** normal (local search) / sequential
- **init_action (action-search seed):** empty (do-nothing) / policy (warmstart heuristic's action)
- **Buffer management:** FIFO / worst-errors (`lowest_error`) / stochastic knockout
- **VFA:** XGBoost / neural net

Full grid = 2 × 2 × 3 × 2 = **24 configs**, named `i10p_adp_{normal,seq}_{empty,policy}_{fifo,lowesterror,knockout}_{xgb,nn}.json`.
All 24 are generated by [configs/gen_phase1_adp_grid.py](configs/gen_phase1_adp_grid.py) (re-run to
regenerate). **Warmstart is now a constant, not a grid axis:** *all* 24 cells seed the replay buffer
with **200k heuristic transitions** (`n_warmstart_states`, == buffer capacity). The `empty`/`policy`
label instead controls **`init_action`** (`agent.extra.init_action`): `empty` runs the local action
search from do-nothing; `policy` seeds it from the warmstart heuristic's action at each decision (same
heuristic for both buffer warmstart and the search seed).

**(ii) MC rollout** — [i10p_rollout_*.json](configs/), with `empty` vs `policy` initial action, and
a sequential variant (`seq_rollout`).

**(iii) PPO with curriculum learning** — curriculum-based policy-gradient,
[i10p_ppo_curriculum.json](configs/i10p_ppo_curriculum.json).

---

## Experiment 1 — Wide selection on a larger instance

Take the algorithm variants that proved viable in Exp 0 and run them on a **larger instance** — likely
Sioux Falls with **~30 projects (N≈30)**. This is the first experiment that produces statistical
results: **5 seeds per config** with rliable aggregation. Config selection and the instance itself are
**TBD pending Exp 0 results** — generate the N≈30 instance with `experiments/generate_instance.py`,
then derive `i30p_*` configs from the surviving Exp 0 configs.

---

## Experiment 2 — Scale-up case study

Only the **best** algorithms from Exp 1, on a large case-study network. **Details TBD.** (Amsterdam
network interface exists but `load_amsterdam` is an unimplemented stub.)

---

## Current Status

Source of truth for the queue: [hpc/registry.json](hpc/registry.json) — **37 entries** (1 replication
each). Conventions: [hpc/registry_conventions.md](hpc/registry_conventions.md). Status column is
**manual.** All build-test OK. Everything runs on the **regenerated, predictable `instance_10p`**
(noisy-instance results archived under `results/exp0_noisy_alpha0p05/`).

**Results layout:** runs write to **`results/exp0/<run_name>/`** (config `run_name = exp0/...`). Old
pre-restructure dirs (`exp_rollout_*`, `heuristics_experiment`, …) are stale clutter at `results/` top
level, left untouched.

**Dispatch:** **native SLURM array** ([hpc/submit_array.sh](hpc/submit_array.sh)) — disconnect-proof
(no HQ login-node server, which killed an earlier run). Single-node jobs share nodes; QOS allows 128
concurrent. `n_workers=16` / `--cpus-per-task=16` (min billed slot), no `--mem-per-cpu`. HQ scripts
kept as fallback. See [hpc/snellius_reference.md](hpc/snellius_reference.md).

**Registry index map:** ppo=0 · ADP=1-24 · MC rollout=25-28 · original optuna=29-31 · **new heuristics
(expanded 0A)=32-36** (`holding, leadtime, netconcurrency, valuedensity, worstfirst`).

**Live plan (pipeline; each stage needs an upload + login-node `build_experiment` canary first):**
```
# 0A — DONE on predictable instance. All 8 heuristics tuned; per-asset is the
#      DECISIVE winner (795M held-out, CV 0.19; next best netconcurrency 933M).
# Winner (per-asset) ALREADY injected into all 0B consumers:
#   - 24 ADP warmstart blocks   (--targets "configs/i10p_adp_*.json"     --block warmstart)
#   - 4  rollout base policies   (--targets "configs/i10p_*rollout_*.json" --block rollout_policy)
#   - PPO phase-0 imitation      (--targets "configs/i10p_ppo_curriculum.json" --block curriculum_heuristic)
# Ready to run 0B:
sbatch --array=2     hpc/submit_array.sh   # smoke one NN ADP cell (expect ~1e9, ACTING — NN fix)
sbatch --array=0-28  hpc/submit_array.sh   # ppo + 24 ADP + 4 rollout, 28h walltime
```
NN/PPO normalization, rollout horizon fix, PPO eval-writing, ADP `horizon_rollout`, the warmstart=
all-cells + `init_action` split, and PPO phase-0 per-asset cloning are all in.

### 0a. Optuna heuristics — DONE on the PREDICTABLE instance (all 8 tuned, 2026-06-20)
WilcoxonPruner + 100 tuning eps; ~11k–32k trials each, clean. **Per-asset is the decisive winner**
(15% better than the next heuristic, 3× lower variance). Held-out = 50 CRN episodes, T+tail:
| Run | held-out cost | CV | Run | held-out cost | CV |
|---|---|---|---|---|---|
| **perasset** ✅ **best** | **795M** | **0.19** | worstfirst | 1133M | 0.73 |
| netconcurrency | 933M | 0.58 | paced | 1244M | 0.77 |
| valuedensity | 938M | 0.59 | leadtime | 1315M | 0.62 |
| holding | 960M | 0.44 | reactive | 1535M | 0.59 |

The 5 new heuristics (registry idx **32-36**: `holding`, `leadtime`, `netconcurrency`, `valuedensity`,
`worstfirst`) did **not** beat per-asset — even the network-aware `netconcurrency` is 17% behind.
**Per-asset is injected into all 0B consumers** (24 ADP warmstart + 4 rollout base + PPO phase-0).
*(Noisy-instance 0A: reactive best 830M — archived under `results/exp0_noisy_alpha0p05/`.)*

### 0b-i. ADP grid — **24/24 built, per-asset injected** · ✅ ready to run
All cells of `{normal,seq} × {empty,policy} × {fifo,lowesterror,knockout} × {xgb,nn}`. Run names:
`exp0/i10p_adp_{ag}_{init}_{buf}_{vfa}`. **All 24 cells warmstart the buffer with 200k per-asset
transitions** (constant). The `empty`/`policy` axis is now **`init_action`**: `policy` cells seed the
local action search from the per-asset heuristic's action; `empty` cells search from do-nothing.
**Training-mode change for the re-run (2026-06-19):** switched `truncation_mode` `bootstrap →
horizon_rollout` (non-circular target — the ADP-XGB diagnosis showed bootstrap masks calibration), and
**fixed the trainer** so horizon_rollout actually simulates the tail in training (it previously broke
at `done`=T → was identical to `none`; now collects T+tail transitions — verified). Added
`eval_interval_seconds: 3600` (hourly policy curve to `training_log.csv`) since episode-based eval is
effectively off. *Noisy-instance ADP was at the foresight ceiling (not buggy) — the predictable
instance is the real test.*

**24h-run post-mortem (2026-06-22) — two separate failures diagnosed, one fixed:**

- **XGB-ADP was throughput-starved (FIXED).** Only ~1,050 episodes in 24h (~45/h) vs NN's ~8,800/h.
  cProfile of one greedy episode showed **78% of wall time in `value_fn.predict`** — specifically
  XGBoost's *per-call* fixed overhead (`config_context` + array-interface marshalling), because
  `LocalSearchGenerator` evaluated candidates **one at a time** (6,785 single-row predict calls per
  240-step episode). Fix: rewrote `LocalSearchGenerator` to **steepest-descent with one batched
  `predict` per sweep** (mirrors `SequentialGenerator`). One episode 6.38s → **1.15s (5.5×)**; XGB
  predict-time 5.0s → 0.30s. Behaviour note: first-improvement → steepest-descent (same neighbourhood
  / same Q / same termination; not bit-identical to the pre-batch trajectory). Tests added.

- **NN-ADP does not converge — ROOT CAUSE is action discrimination, NOT online instability (open).**
  The "wander" (1.5B–4.1B, never near the 785M heuristic) is a *broken policy from the very first
  eval*: a freshly warmstarted+pretrained NN-ADP evals at **3.5e10–4.3e10 (~50× worse than its 785M
  warmstart heuristic) before any online drift**. Offline diagnosis on warmstart data:
  - The MC future-only target `mc_return − cost` has **CV ≈ 0.74** (std ~2.7e8 at mean ~3.7e8); even
    within a single epoch-bucket the std is ~1.2e8 — **the same magnitude as a renovation's immediate
    cost (~1.2e8)**. So an R²≈0.8 fit (which the VF achieves) still leaves residual noise equal to the
    action signal it must resolve → `V'(renovate) − V'(none)` spread = −6.5e7 with **std 9.2e7 > |mean|**.
  - The warmstart buffer is **97.8% do-nothing** (and `none` even at 95% of d>0.9 near-failure slots),
    so the VF is barely trained on the consequences of *acting* — exactly the post-states the greedy
    search must rank. TD-bootstrap targets help only marginally (renovate-preferred 25%→32%).
  - Conclusion: ADP-NN as currently formulated cannot rank act-vs-wait on this instance. Candidate
    structural fixes (not yet applied — they change the comparison and need a design decision):
    (a) lower-variance targets (multi-sample/averaged MC, or advantage-style relative targets),
    (b) more act-at-high-d coverage in warmstart (raise the exploration `p_flip`), (c) train V' to
    predict a *centered* cost-to-go so the action delta isn't a small difference of large numbers.
  - **Recommendation:** XGB-ADP is now fast enough to be a fair cell — keep it and re-run. NN-ADP
    should be treated as "investigated, does not converge under MC-ADP targets" — either drop it from
    the grid or re-run only after a target-variance fix lands. Reporting it as-is would just show a
    50× regression with no learning signal.

### 0b-ii. MC rollout — 4/4 built · ⚠️ first run was buggy → FIXED, needs re-run
First 0B run produced **pathological ~10B eval (99% risk, do-nothing)**. Root cause (found + fixed):
a **vanishing rollout horizon** — configs set `max_steps` but `configs.py` read `rollout_horizon`, so
the window was dropped and `_estimate_q` used `max_steps = cfg.T - t_next`, which goes ≤0 across the
eval tail (T+tail) → zero rollouts → do-nothing → escalating C_risk. Fixes in `agents/rollout.py`
(horizon now a fixed t-independent window), `configs.py` (`_check_extra_keys` rejects unknown extras),
and the 4 configs (`max_steps`→`rollout_horizon: 100`). Local CRN validation: 3.9e9/92%-risk →
~5.4e8/~6%-risk (vs reactive base 3.6e8 at shortened T). Uses **adaptive sequential-Wilcoxon**
budgeting (`rollout_selection: adaptive`, `p_threshold 0.02`, `min/max_rollouts 20/100`).
**Open:** full 50-ep Snellius eval not yet run — re-run to confirm it lands near/below 830M base; may
only *match* base at this budget (consider higher `n_rollouts` / less-degenerate base ~0.7 to beat it).
Base policy is now the **per-asset** 0A winner (placeholder; re-injected with the expanded-0A winner).
The "830M/reactive base" figures above are noisy-instance history; the predictable-instance bar is
per-asset **795M**.
| Run | In registry | Status |
|---|---|---|
| i10p_rollout_empty | ✅ | 🔧 fixed, HELD |
| i10p_rollout_policy | ✅ | 🔧 fixed, HELD |
| i10p_seq_rollout_empty | ✅ | 🔧 fixed, HELD |
| i10p_seq_rollout_policy | ✅ | 🔧 fixed, HELD |

### 0b-iii. PPO curriculum — 1/1 built · 🟢 submitted (array 0, stage 0B-1)
| Run | In registry | Status |
|---|---|---|
| i10p_ppo_curriculum | ✅ | 🟢 submitted |

---

## Compute Budget (from [hpc/experiment_setup.md](hpc/experiment_setup.md))

- **Replication:** Exp 0 = **1 per config** (exploration). Exp 1 / Exp 2 = **5 seeds** per config
  (report with `rliable`: stratified bootstrap CIs). **Optuna tuning is never replicated** (1 run).
  See [hpc/registry_conventions.md](hpc/registry_conventions.md).
- **Workers:** learner configs use `n_workers = 16` with `--cpus-per-task=16` (the Snellius minimum
  shareable slot, so it's the smallest billed unit anyway — see
  [hpc/registry_conventions.md](hpc/registry_conventions.md); no `--mem-per-cpu`). **Optuna heuristic
  search is single-threaded** (`n_workers = 1`) — sequential, ignores extra workers. Use `n_workers:
  1` everywhere on the laptop.
- **Walltime:** 24 h per run (`time_budget: 86400`).
- Runs dispatched via HyperQueue; `hpc/run_task.py` applies the per-entry `seed`/`run_name` overrides
  from the registry.

---

<a id="gaps--open-items"></a>
## Gaps / Open Items

- **0A→0B injection: DONE.** Tuned v2 **reactive** params (thr 0.99997, repair 0.924, restrict 0.334)
  injected via `apply_optuna_params.py` into the 12 ADP `*_policy_*` warmstart blocks and the 4
  rollout `rollout_policy` blocks. All 16 build OK.
- **⚠️ MC rollout runtime exceeds 24h walltime (blocks 0B submission for those 4 configs).** Build-time
  estimate on `instance_10p`: `i10p_rollout_{empty,policy}` ≈ **5 days** for 50 eval episodes
  (2h24m/ep); `i10p_seq_rollout_*` ≈ **40h**. Structural (rollout count = n_rollouts × candidates ×
  decision-epochs), not from the injection. Fix before submitting array 0-28: cut `n_eval_episodes`
  (e.g. 50→10) and/or `n_rollouts` for the rollout configs, or give them a longer walltime. ADP/PPO
  configs are unaffected.
- **Exp 1 instance & config selection TBD** — pending Exp 0 results; generate the N≈30 instance and
  derive `i30p_*` configs from the Exp 0 survivors.
- **Exp 2 case study undefined** — `load_amsterdam` is a stub; decide the large network.
- This status table is hand-maintained. Consider a script to scan `results/` and auto-fill status.
